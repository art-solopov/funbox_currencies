en:
  activerecord:
    attributes:
      currency_rate:
        value: Rate
        forced_until: Force until

  time:
    formats:
      default: "%m/%d/%Y %H:%M"
  
  helpers:
    admin_name: Administration
    submit:
      currency_rate:
        create: Add a currency exchange rate
    
  home:
    show:
      exchange_rate: Exchange rate
      no_exchange: Exchange rate not found
